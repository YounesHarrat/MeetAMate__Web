<form method="post" action="{{ path('auth_profile_delete', {'id': auth_profile.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ auth_profile.id) }}">
    <button class="btn">Delete</button>
</form>
